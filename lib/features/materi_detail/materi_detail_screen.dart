import 'package:flutter/material.dart';

class MateriDetailScreen extends StatelessWidget {
  const MateriDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Mater Detail Screen'),
        
      ),
    );
  }
}
